from setuptools import setup, find_packages

setup(
    name='wafer_fault_prediction',
    version='1.0',
    description='Prediction if a wafer faults or not from the sensor data',
    packages=find_packages()
)